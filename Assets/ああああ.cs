using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ああああ : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //テストです  更新確認

    }

    // Update is called once per frame
    void Update()
    {
        //こーど
    }
}
